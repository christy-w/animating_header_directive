import { ShrinkingHeaderDirective } from './shrinking-header.directive';

describe('ShrinkingHeaderDirective', () => {
  it('should create an instance', () => {
    const directive = new ShrinkingHeaderDirective();
    expect(directive).toBeTruthy();
  });
});
